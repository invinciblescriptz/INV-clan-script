local library = loadstring(game:HttpGet("https://raw.githubusercontent.com/memejames/elerium-v2-ui-library/main/Library"))()
local window = library:AddWindow("Invincibles Priv Script", {
    main_color = Color3.fromRGB(0, 0, 0),
    min_size = Vector2.new(660,780),
    can_resize = false,
})

local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Players = game:GetService("Players")
local LP = Players.LocalPlayer
local UserInputService = game:GetService("UserInputService")
local workspace = game:GetService("Workspace")

-- Assuming getg() is a global table
if not _G then _G = {} end
local getg = function() return _G end

-- Wait for muscleEvent to exist
local muscleEvent
task.spawn(function()
    repeat
        muscleEvent = LP:FindFirstChild("muscleEvent") or workspace:FindFirstChild("muscleEvent")
        wait(1)
    until muscleEvent
    print("muscleEvent found:", muscleEvent)
end)

-- Utility: firetouchinterest
local function fireTouch(part1, part2, toggle)
    if firetouchinterest then
        firetouchinterest(part1, part2, toggle)
    else
        print("firetouchinterest not found")
    end
end

-- Teleport function
local function tpTo(pos)
    local hrp = LP.Character and LP.Character:FindFirstChild("HumanoidRootPart")
    if hrp then
        hrp.CFrame = CFrame.new(pos)
        print("Teleported to", pos)
    end
end

-- UI Tabs
local main = window:AddTab("Main")
local ToolsFolder = main:AddFolder("Tools")
local TeleportTab = window:AddTab("Teleport")
local KillTab = main:AddTab("Kill")
local farmTab = main:AddTab("Farm") -- For farming tools

-- Teleport Buttons
local teleportButtons = {
    {"To Spawn / Safe Area", Vector3.new(-6, 5, -196)},
    {"To Secret Area", Vector3.new(1947, 2, 6191)},
    {"To Tiny Area", Vector3.new(-39, 6, 1886)},
    {"To Frozen Area", Vector3.new(-2623, 6, -409)},
    {"To Mythical Area", Vector3.new(2251, 6, 1073)},
    {"To Inferno Area", Vector3.new(-6759, 6, -1285)},
    {"To Legend Area", Vector3.new(4603, 990, -3898)},
    {"To Muscle King Area", Vector3.new(-8626, 16, -5730)},
    {"To Jungle Area", Vector3.new(-8642, 6, 2381)},
    {"To Lava Brawl", Vector3.new(4471, 119, -8836)},
    {"To Desert Brawl", Vector3.new(960, 17, -7398)},
    {"To Beach Brawl", Vector3.new(-1849, 20, -6335)}
}

for _, btn in pairs(teleportButtons) do
    local name, position = unpack(btn)
    TeleportTab:AddButton(name, function()
        tpTo(position)
    end)
end

-- Auto Kill All Players
local killAll = false
local function killLoop()
    while killAll do
        for _, plr in pairs(Players:GetPlayers()) do
            if plr ~= LP and plr.Character then
                local hrp_target = plr.Character:FindFirstChild("HumanoidRootPart")
                local hrp_self = LP.Character and LP.Character:FindFirstChild("HumanoidRootPart")
                if hrp_target and hrp_self then
                    fireTouch(hrp_target, hrp_self, 0)
                    fireTouch(hrp_target, hrp_self, 1)
                end
            end
        end
        wait(0.1)
    end
end

local killFolder = KillTab:AddFolder("Auto Kill")
killFolder:AddSwitch("Kill All Players", function(state)
    killAll = state
    if state then
        task.spawn(killLoop)
    end
end)

local targetName = nil
local dropdown = killFolder:AddDropdown("Target Player", {
    Values = {},
    Callback = function(val)
        targetName = val
    end
})

killFolder:AddSwitch("Auto Kill Target", function(state)
    _G.autoKillTarget = state
    if state and targetName then
        task.spawn(function()
            while _G.autoKillTarget do
                local tgt = Players:FindFirstChild(targetName)
                if tgt and tgt.Character then
                    local hrp_target = tgt.Character:FindFirstChild("HumanoidRootPart")
                    local hrp_self = LP.Character and LP.Character:FindFirstChild("HumanoidRootPart")
                    if hrp_target and hrp_self then
                        fireTouch(hrp_target, hrp_self, 0)
                        fireTouch(hrp_target, hrp_self, 1)
                    end
                end
                wait(0.08)
            end
        end)
    end
end)

-- Update dropdown list of players periodically
task.spawn(function()
    while true do
        local list = {}
        for _, plr in pairs(Players:GetPlayers()) do
            if plr ~= LP then
                table.insert(list, plr.Name)
            end
        end
        pcall(function()
            dropdown:SetValues(list)
        end)
        wait(5)
    end
end)

-- Auto farm rocks
local autoFarm = false
local function autoFarmRocks(durability)
    while autoFarm do
        local machinesFolder = workspace:FindFirstChild("machinesFolder")
        if machinesFolder then
            for _, v in pairs(machinesFolder:GetDescendants()) do
                if v.Name == "neededDurability" and v.Value == durability then
                    local rock = v.Parent and v.Parent:FindFirstChild("Rock")
                    local char = LP.Character
                    if rock and char and char:FindFirstChild("RightHand") and char:FindFirstChild("LeftHand") then
                        fireTouch(rock, char.RightHand, 0)
                        fireTouch(rock, char.RightHand, 1)
                        fireTouch(rock, char.LeftHand, 0)
                        fireTouch(rock, char.LeftHand, 1)
                        -- Punch while farming
                        if _G.FastPunch then
                            if muscleEvent then
                                muscleEvent:FireServer("punch", "rightHand")
                                muscleEvent:FireServer("punch", "leftHand")
                            end
                            local punch = char:FindFirstChild("Punch")
                            if punch then punch:Activate() end
                        end
                    end
                end
            end
        end
        wait(0.15)
    end
end

local RockFarmFolder = farmTab:AddFolder("Rock Farm")
RockFarmFolder:AddSwitch("Farm Rock (durability 0)", {
    Default = false,
    Callback = function(val)
        autoFarm = val
        if val then
            task.spawn(function() autoFarmRocks(0) end)
        end
    end
})

-- Fast Punch with auto-equip
ToolsFolder:AddSwitch("Fast Punch", function(state)
    _G.FastPunch = state
    if state then
        task.spawn(function()
            local function equipPunch()
                local ch = LP.Character
                if not ch then return end
                local punch = LP.Backpack:FindFirstChild("Punch")
                if punch then
                    punch.Parent = ch
                end
            end
            equipPunch()
            while _G.FastPunch do
                pcall(function()
                    if muscleEvent then
                        muscleEvent:FireServer("punch", "rightHand")
                        muscleEvent:FireServer("punch", "leftHand")
                    end
                    local ch = LP.Character
                    if ch and not ch:FindFirstChild("Punch") then
                        local punch = LP.Backpack:FindFirstChild("Punch")
                        if punch then punch.Parent = ch end
                    end
                    local punch = ch and ch:FindFirstChild("Punch")
                    if punch then punch:Activate() end
                end)
                wait(0.07)
            end
        end)
    end
end)
