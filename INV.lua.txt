-- Invincible Free - Safer 2026 version (fixed)
-- Use on alt | Test slowly | Avoid enabling everything at once

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local VirtualUser = game:GetService("VirtualUser")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Lighting = game:GetService("Lighting")
local LocalPlayer = Players.LocalPlayer

-- Random UI color
local r = math.random(60, 220)
local g = math.random(60, 220)
local b = math.random(60, 220)
local randomColor = Color3.fromRGB(r, g, b)

-- UI Library
local library = loadstring(game:HttpGet("https://raw.githubusercontent.com/memejames/elerium-v2-ui-library/main/Library", true))()
local window = library:AddWindow("|| INV public ||", {
    main_color = randomColor,
    min_size = Vector2.new(480, 480),
    can_resize = false,
    background_transparency = 0.95,
})

getgenv().NexusRunning = true

local states = {
    AutoPushups = false,
    AutoWeight = false,
    AutoHandstands = false,
    FastPunch = false,
    InfiniteJump = false,
    AutoRebirth = false,
    AutoRebirthTarget = false,
    AntiAfk = false,
    RockFarm = false,
}

local targetRebirths = 1
getgenv().rockFarmActive = false
getgenv().selectedRockDurability = 0

-- Helpers
local muscleEvent = ReplicatedStorage:FindFirstChild("muscleEvent", true) -- probably wrong in 2026

local function safeTp(pos)
    pcall(function()
        local hrp = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
        if hrp then
            hrp.CFrame = CFrame.new(pos + Vector3.new(0, 3, 0))
        end
    end)
end

local function equipAndActivate(toolName)
    pcall(function()
        local char = LocalPlayer.Character
        if not char or not char:FindFirstChild("Humanoid") or char.Humanoid.Health <= 0 then return end
        
        local tool = LocalPlayer.Backpack:FindFirstChild(toolName) or char:FindFirstChild(toolName)
        if tool then
            if tool.Parent ~= char then
                char.Humanoid:EquipTool(tool)
                task.wait(0.18)
            end
            tool:Activate()
        end
    end)
end

-- Safer auto tool farm
local function startAutoToolFarm(toolName, stateKey)
    task.spawn(function()
        while states[stateKey] and getgenv().NexusRunning do
            equipAndActivate(toolName)
            task.wait(0.55 + math.random(0, 80) / 100)
        end
    end)
end

-- Infinite Jump
local jumpConn
local function toggleInfiniteJump(state)
    if state then
        if jumpConn then return end
        jumpConn = UserInputService.JumpRequest:Connect(function()
            pcall(function()
                local hum = LocalPlayer.Character and LocalPlayer.Character:FindFirstChildOfClass("Humanoid")
                if hum then hum:ChangeState(Enum.HumanoidStateType.Jumping) end
            end)
        end)
    else
        if jumpConn then jumpConn:Disconnect() jumpConn = nil end
    end
end

-- Anti-AFK
local antiAfkConn
local function toggleAntiAfk(state)
    if state then
        if antiAfkConn then return end
        antiAfkConn = LocalPlayer.Idled:Connect(function()
            VirtualUser:CaptureController()
            VirtualUser:ClickButton2(Vector2.new())
        end)
    else
        if antiAfkConn then antiAfkConn:Disconnect() antiAfkConn = nil end
    end
end

-- Rock farm loop (very slow)
task.spawn(function()
    while getgenv().NexusRunning do
        if not getgenv().rockFarmActive or getgenv().selectedRockDurability <= 0 then
            task.wait(1.2)
            continue
        end
        
        pcall(function()
            local char = LocalPlayer.Character
            if not char or char.Humanoid.Health <= 0 then task.wait(2.5) return end
            
            local dur = LocalPlayer:FindFirstChild("Durability")
            if not dur or dur.Value < getgenv().selectedRockDurability then
                task.wait(1.8)
                return
            end
            
            equipAndActivate("Punch")
            
            for _, obj in pairs(workspace.machinesFolder:GetDescendants()) do
                if obj.Name == "neededDurability" and obj:IsA("IntValue") and obj.Value == getgenv().selectedRockDurability then
                    local rock = obj.Parent:FindFirstChild("Rock")
                    if rock and rock:IsA("BasePart") then
                        local hand = char:FindFirstChild("RightHand") or char:FindFirstChild("LeftHand")
                        if hand then
                            firetouchinterest(rock, hand, 0)
                            task.wait(0.09 + math.random(0, 30)/100)
                            firetouchinterest(rock, hand, 1)
                        end
                        task.wait(0.7 + math.random(20, 90)/100)
                        break
                    end
                end
            end
        end)
        
        task.wait(1.1 + math.random(30, 120)/100)
    end
end)

local function setRockFarm(dur, enable)
    if enable then
        getgenv().selectedRockDurability = dur
        getgenv().rockFarmActive = true
    else
        if getgenv().selectedRockDurability == dur then
            getgenv().rockFarmActive = false
            getgenv().selectedRockDurability = 0
        end
    end
end

-- Safer rebirth
local function tryRebirth()
    pcall(function()
        local rEvents = ReplicatedStorage:FindFirstChild("rEvents")
        if rEvents and rEvents:FindFirstChild("rebirthRemote") then
            rEvents.rebirthRemote:InvokeServer("rebirthRequest")
        end
    end)
end

local rebirthLoop
local function toggleAutoRebirth(state)
    states.AutoRebirth = state
    if state then
        rebirthLoop = task.spawn(function()
            while states.AutoRebirth and getgenv().NexusRunning do
                local rebirths = LocalPlayer.leaderstats and LocalPlayer.leaderstats:FindFirstChild("Rebirths")
                if rebirths then
                    tryRebirth()
                end
                task.wait(2.8 + math.random(100, 400)/100)
            end
        end)
    else
        if rebirthLoop then task.cancel(rebirthLoop) rebirthLoop = nil end
    end
end

-- ──────────────────────────────────────────────────────────────
-- UI Tabs
-- ──────────────────────────────────────────────────────────────

local mainTab     = window:AddTab("Main")
local rebirthTab  = window:AddTab("Rebirth")
local teleportTab = window:AddTab("Teleport")
local creditsTab  = window:AddTab("Credits")
local killTab     = window:AddTab("Kill")   -- fixed typo

local toolsFolder = mainTab:AddFolder("Auto Tools")
local rockFolder  = mainTab:AddFolder("Rock Farm")

-- Global toggles / vars used in Kill tab
_G.autoGoodKarma      = false
_G.autoBadKarma       = false   -- never used, can remove
_G.fastHitActive      = false
_G.whitelistedPlayers = _G.whitelistedPlayers or {}
_G.viewPlayer         = false
_G.ViewTarget         = ""
_G.autoWhitelistFriends = false
_G.selectedPet        = ""

-- Player list update (for dropdowns)
local function UpdatePlayerLists()
    WhitelistDropdown:Clear()
    ViewDropdown:Clear()
    for _, plr in ipairs(Players:GetPlayers()) do
        if plr ~= LocalPlayer then
            WhitelistDropdown:Add(plr.DisplayName)
            ViewDropdown:Add(plr.DisplayName)
        end
    end
end

Players.PlayerAdded:Connect(UpdatePlayerLists)
Players.PlayerRemoving:Connect(UpdatePlayerLists)
task.wait(0.5)
UpdatePlayerLists()

-- Kill Tab content
killTab:AddLabel("Select damage or durability pet")
local PetSelector = killTab:AddDropdown("Select Pet", function(Value)
    _G.selectedPet = Value
end)
PetSelector:Add("Wild Wizard")
PetSelector:Add("Mighty Monster")

killTab:AddSwitch("Auto Good Karma", function(bool)
    _G.autoGoodKarma = bool
    task.spawn(function()
        while _G.autoGoodKarma do
            pcall(function()
                local char = LocalPlayer.Character
                if char then
                    local tool = char:FindFirstChild("GoodKarma") or LocalPlayer.Backpack:FindFirstChild("GoodKarma")
                    if tool then
                        char.Humanoid:EquipTool(tool)
                        tool:Activate()
                    end
                end
            end)
            task.wait(0.1)
        end
    end)
end):Set(false)

killTab:AddSwitch("Auto Whitelist Friends", function(bool)
    _G.autoWhitelistFriends = bool
    task.spawn(function()
        while _G.autoWhitelistFriends do
            pcall(function()
                for _, plr in ipairs(Players:GetPlayers()) do
                    if plr ~= LocalPlayer and LocalPlayer:IsFriendsWith(plr.UserId) then
                        if not table.find(_G.whitelistedPlayers, plr.Name) then
                            table.insert(_G.whitelistedPlayers, plr.Name)
                        end
                    end
                end
            end)
            task.wait(5)
        end
    end)
end):Set(false)

local WhitelistDropdown = killTab:AddDropdown("Add to Whitelist", function(displayName)
    for _, plr in ipairs(Players:GetPlayers()) do
        if plr.DisplayName == displayName and not table.find(_G.whitelistedPlayers, plr.Name) then
            table.insert(_G.whitelistedPlayers, plr.Name)
            break
        end
    end
end)

killTab:AddSwitch("Auto Punch", function(bool)
    _G.fastHitActive = bool
    task.spawn(function()
        while _G.fastHitActive do
            equipAndActivate("Punch")
            task.wait(0.1)
        end
    end)
end):Set(false)

-- ... (rest of Kill tab features like Remove Punch Anim, View Player, Size buttons, Time changer, etc.)

-- Example fix for size buttons (using correct service)
killTab:AddButton("Size 30", function()
    pcall(function()
        ReplicatedStorage:WaitForChild("rEvents"):WaitForChild("changeSpeedSizeRemote"):InvokeServer("changeSize", 30)
    end)
end)

killTab:AddButton("Size 2", function()
    pcall(function()
        ReplicatedStorage:WaitForChild("rEvents"):WaitForChild("changeSpeedSizeRemote"):InvokeServer("changeSize", 2)
    end)
end)

-- Time changer (fixed Lighting)
local TimeDropdown = killTab:AddDropdown("Change Time", function(value)
    Lighting.Brightness = 2
    Lighting.FogEnd = 100000
    Lighting.Ambient = Color3.fromRGB(127, 127, 127)
    
    local times = {
        Midnight       = 0,
        Morning        = 8,
        Noon           = 12,
        Afternoon      = 14,
        Sunset         = 18,
        Night          = 20,
        Dawn           = 6,
        ["Early Morning"] = 5,
    }
    if times[value] then
        Lighting.ClockTime = times[value]
    end
end)

for _, t in ipairs({"Morning", "Noon", "Afternoon", "Sunset", "Night", "Midnight", "Dawn", "Early Morning"}) do
    TimeDropdown:Add(t)
end

-- Blacklist (commented - most executors don't have file functions)
--[[
local BlacklistFileName = "GenesisBlacklist_" .. LocalPlayer.Name .. ".txt"
if not isfile(BlacklistFileName) then writefile(BlacklistFileName, "") end

killTab:AddTextBox("Add to Blacklist", function(text)
    appendfile(BlacklistFileName, "," .. text)
end, {placeholder = "Ex: User1, User2"})
--]]

-- Credits
creditsTab:AddLabel("Script Made by: invincible")
creditsTab:AddButton("Copy Discord Link", function()
    setclipboard("https://discord.gg/dhySgMQx")
end)

creditsTab:AddParagraph("Note", "Use alt • Slow farming = safer • Remote spam is mostly patched in 2026")

-- Tool toggles
toolsFolder:AddSwitch("Auto Pushups",    function(s) states.AutoPushups = s    if s then startAutoToolFarm("Pushups",    "AutoPushups")    end end)
toolsFolder:AddSwitch("Auto Weight",     function(s) states.AutoWeight = s     if s then startAutoToolFarm("Weight",     "AutoWeight")     end end)
toolsFolder:AddSwitch("Auto Handstands", function(s) states.AutoHandstands = s if s then startAutoToolFarm("Handstands", "AutoHandstands") end end)

toolsFolder:AddSwitch("Auto Punch (Safe)", function(s)
    states.FastPunch = s
    if s then
        task.spawn(function()
            while states.FastPunch and getgenv().NexusRunning do
                equipAndActivate("Punch")
                task.wait(0.48 + math.random(20, 90)/100)
            end
        end)
    end
end)

toolsFolder:AddSwitch("Infinite Jump", toggleInfiniteJump)
toolsFolder:AddSwitch("Anti-AFK",      toggleAntiAfk)

toolsFolder:AddButton("Anti-Lag (FPS Boost)", function()
    Lighting.GlobalShadows = false
    Lighting.Brightness = 1.2
    Lighting.ClockTime = 14
    Lighting.FogEnd = 99999
    
    for _, v in pairs(workspace:GetDescendants()) do
        if v:IsA("ParticleEmitter") or v:IsA("Smoke") or v:IsA("Fire") or v:IsA("Sparkles") or
           v:IsA("BloomEffect") or v:IsA("BlurEffect") or v:IsA("DepthOfFieldEffect") or v:IsA("SunRaysEffect") then
            v.Enabled = false
        end
    end
    
    settings().Rendering.QualityLevel = Enum.QualityLevel.Level01
end)

-- Teleports
teleportTab:AddButton("Spawn / Safe",        function() safeTp(Vector3.new(-6,    5,   -196))   end)
teleportTab:AddButton("Secret Area",         function() safeTp(Vector3.new(1947,  2,   6191))   end)
teleportTab:AddButton("Tiny Area",           function() safeTp(Vector3.new(-39,   6,   1886))   end)
teleportTab:AddButton("Frozen Area",         function() safeTp(Vector3.new(-2623, 6,   -409))   end)
teleportTab:AddButton("Mythical Area",       function() safeTp(Vector3.new(2251,  6,   1073))   end)
teleportTab:AddButton("Inferno Area",        function() safeTp(Vector3.new(-6759, 6,   -1285))  end)
teleportTab:AddButton("Legend Area",         function() safeTp(Vector3.new(4603,  990, -3898))  end)
teleportTab:AddButton("Muscle King Area",    function() safeTp(Vector3.new(-8626, 16,  -5730))  end)
teleportTab:AddButton("Jungle Area",         function() safeTp(Vector3.new(-8642, 6,   2381))   end)

-- Rock Farm switches
rockFolder:AddSwitch("Farm Tiny Island Rock (0)",        function(s) setRockFarm(0,         s) end)
rockFolder:AddSwitch("Farm Starter Rock (100)",          function(s) setRockFarm(100,       s) end)
rockFolder:AddSwitch("Farm Legend Beach Rock (5000)",    function(s) setRockFarm(5000,      s) end)
rockFolder:AddSwitch("Farm Frost Gym Rock (150k)",       function(s) setRockFarm(150000,    s) end)
rockFolder:AddSwitch("Farm Mythical Gym Rock (400k)",    function(s) setRockFarm(400000,    s) end)
rockFolder:AddSwitch("Farm Eternal Gym Rock (750k)",     function(s) setRockFarm(750000,    s) end)
rockFolder:AddSwitch("Farm Legend Gym Rock (1M)",        function(s) setRockFarm(1000000,   s) end)
rockFolder:AddSwitch("Farm Muscle King Rock (5M)",       function(s) setRockFarm(5000000,   s) end)
rockFolder:AddSwitch("Farm Ancient Jungle Rock (10M)",   function(s) setRockFarm(10000000,  s) end)

rockFolder:AddButton("STOP Rock Farming", function()
    getgenv().rockFarmActive = false
    getgenv().selectedRockDurability = 0
end)

-- Rebirth tab
local rebirthFolder = rebirthTab:AddFolder("Rebirth Controls")
rebirthFolder:AddSwitch("Auto Rebirth (Slow)", toggleAutoRebirth)

rebirthFolder:AddInput("Target Rebirths", function(value)
    local num = tonumber(value)
    targetRebirths = num and math.max(1, num) or 1
end)

rebirthFolder:AddSwitch("Auto Rebirth → Target", function(s)
    states.AutoRebirthTarget = s
    if s then
        task.spawn(function()
            while states.AutoRebirthTarget and getgenv().NexusRunning do
                local rebirths = LocalPlayer.leaderstats and LocalPlayer.leaderstats:FindFirstChild("Rebirths")
                if rebirths and rebirths.Value >= targetRebirths then
                    states.AutoRebirthTarget = false
                    return
                end
                tryRebirth()
                task.wait(3.2 + math.random(80, 300)/100)
            end
        end)
    end
end)

-- Cleanup
game:BindToClose(function()
    getgenv().NexusRunning = false
end)
